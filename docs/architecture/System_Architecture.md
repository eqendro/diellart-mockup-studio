# System Architecture

**Draft — to be expanded.**

The initial application is a browser-based Next.js interface with no database, authentication, upload processing, or server-side storage. This document will evolve as those capabilities are designed.

