export type ArtworkPlacement = {
  scale: number;
  offsetX: number;
  offsetY: number;
};

export type PlacementLimits = {
  minimumScale: number;
  maximumScale: number;
  minimumOffsetX: number;
  maximumOffsetX: number;
  minimumOffsetY: number;
  maximumOffsetY: number;
};
