# Configuration

Typed application configuration is grouped by concern:

- `app` for application-level settings
- `products` for verified product definitions
- `theme` for TypeScript-facing theme configuration when CSS tokens alone are insufficient

